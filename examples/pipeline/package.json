{
  "name": "@agent-receipts/example-pipeline",
  "version": "0.1.1",
  "private": true,
  "description": "Document analysis pipeline with chained receipts and constraints",
  "scripts": {
    "start": "npx tsx index.ts"
  },
  "dependencies": {
    "@agent-receipts/sdk": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
